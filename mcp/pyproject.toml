[project]
name = "spendguard-mcp"
version = "1.0.0"
description = "MCP server for SpendGuard financial authorization API"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
spendguard-mcp = "server:main"
